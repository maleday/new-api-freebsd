name: Auto Sync Upstream

permissions:
  contents: write  # 授予写权限，以便合并代码

on:
  schedule:
    # 每天国际标准时间 0点15分（北京时间早上8点15分）运行一次
    - cron: "15 0 * * *"

  workflow_dispatch: # 手动运行按钮

jobs:
  sync_latest_from_upstream:
    name: Sync latest commits from upstream repo
    runs-on: ubuntu-latest
    if: ${{ github.event.repository.fork }}

    steps:
      # 1. 检出你的代码
      - name: Checkout target repo
        uses: actions/checkout@v3

      # 2. 同步上游代码
      - name: Sync upstream changes
        id: sync
        uses: aormsby/Fork-Sync-With-Upstream-action@v3.4
        with:
          upstream_sync_repo: k0baya/new-api-freebsd
          upstream_sync_branch: main
          target_sync_branch: main
          target_repo_token: ${{ secrets.GITHUB_TOKEN }}
          test_mode: false

      # 3. 如果同步失败，比如有冲突，输出日志
      - name: Sync check
        if: failure()
        run: |
          echo "[Error] 自动同步失败，可能是因为上游修改了 workflow 文件或产生了代码冲突。"
          echo "[Info] 请手动访问 GitHub 页面进行 Sync。"
          exit 1
